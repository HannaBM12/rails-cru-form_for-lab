<h1>Add Song</h1>

<table>

<%= form_for @song do |f|%>
<tr><td>Song:</td><td><%= f.text_field :name%></td></tr>
<tr><td>Artist:</td><td><%= f.collection_select :artist_id, Artist.all, :id, :name%></td></tr>
<tr><td>Genre:</td><td><%= f.collection_select :genre_id, Genre.all, :id, :name%></td></tr>
<tr><td><%= f.submit%></td></tr>
<%end%>

</table>